export { load } from 'sveltekit-flash-message/server';

// NOTE: Since we're not loading any data here, we can simply re-export the load function
// from the flash message module
// export const load = loadFlash(async () => {
// 	return {};
// });
