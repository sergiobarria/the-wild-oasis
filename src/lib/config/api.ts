/**
 * Default stale time for queries (5 seconds)
 */
export const DEFAULT_STALE_TIME = 5000;

/**
 * Default debounce delay for inputs (500ms)
 */
export const DEFAULT_DEBOUNCE_DELAY = 500;

/**
 * Rate limit for API requests (requests per minute)
 */
export const API_RATE_LIMIT = 60;
